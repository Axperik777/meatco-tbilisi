# MeatCO v8.9 — пять точечных правок

- Home: две короткие строки в hero; ниже одна компактная строка часов, интервалов, минимума и ответа после закрытия, затем курьер/самовывоз. Минимум сокращён только в Home.
- Английские вкладки: Home · Catalog · Cart · Help.
- Грузинские плашки: короткие подписи владельца, 13 px, две равные по высоте строки сетки на телефоне.
- Help: явные ежедневные часы, затем правило после 18:00, самовывоз и минимум. FAQ и ресторанная ссылка сохранены.
- От 1100 px убрано внутреннее ограничение 800 px у главного кадра, условий и категорий. Оболочка, шапка и вкладки остаются на общем полотне до 1120 px; каталог — три колонки.

Изменены `src/shop-view.mjs`, `src/locales.json`, одна строка форматирования в `src/app-core.js`, локальные CSS-правила в `dist/app-shell.css` и генерируемая сборка. Ассортимент, фотографии, цены, корзина, интервалы и WhatsApp-построитель не менялись.

Проверка: общий валидатор проходит; 46 товаров, 393 строки на язык. В браузере проверены 360/390 px KA, английские вкладки и Help, 1100/1366 px. Грузинские подписи занимают 1–2 строки, плашки одинаковой высоты; переполнения нет. На 1366 px оболочка/шапка/вкладки — 1120 px, главный кадр — 1086 px. На 1100 px оболочка и вкладки — 1085 px с учётом полосы прокрутки, каталог — три колонки. Ошибок консоли нет.
